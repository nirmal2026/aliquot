# Envicron
Offline environmental laboratory & field calculator — 91 routines, 7 matrix pages.
Save as PDF: real PDF (jsPDF, offline), file named after the calculation → native Share sheet.
Device-local (IST) timestamps. Not an official product; a calculation aid (ISO/IEC 17025 §7.11.2).
## Build APK: Actions → Build Envicron APK → Run workflow. android/ generated on runner (gitignored).
